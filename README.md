<div align="center">

**🌐 Made by [Sai](https://saaaai.com) · [saaaai.com](https://saaaai.com)** — AI workflow · one homepage

**[English](README.md) · [简体中文](README.zh-CN.md) · [日本語](README.ja.md)**

</div>

# Sai Reader

![Sai Reader hero — local-first Markdown reader for Windows](assets/readme/hero.svg)

A local-first Markdown reader for Windows, built with Tauri 2 (Rust + WebView2). Fast, private, and entirely offline — your notes never leave your machine.

## Features

- **Multi-root library** — register several local folders as document roots; browse each via a sidebar file tree with directory outline
- **Local-first & private** — pure file-system access with a path-cage; no cloud, no telemetry, no accounts
- **Full Markdown rendering** — GitHub-flavored Markdown via [marked](https://github.com/markedjs/marked), math via [KaTeX](https://github.com/KaTeX/KaTeX), code highlighting via [highlight.js](https://github.com/highlightjs/highlight.js)
- **Built-in editor** — edit any document inline and save back to disk
- **Themes** — light/dark theme switching, custom accent colors
- **Open in terminal** — launch an external terminal at the active library root
- **Native installer** — NSIS + MSI bundles generated by Tauri

## Screenshots

<img src="assets/readme/screenshot.png" alt="Sai Reader screenshot" width="100%">

The hero above already previews the actual look — sidebar file tree, paper reader page, and code block, drawn in the app's own color palette. A full window screenshot will be added here.

## Requirements

- Windows 10/11 (WebView2 runtime, installed by default on modern Windows)
- [Rust](https://rustup.rs/) (stable toolchain)
- [Node.js](https://nodejs.org/) 18+

## Build from source

```powershell
npm install
npm run dev       # development build with hot reload
npm run build     # release build → NSIS/MSI installers in src-tauri/target/release/bundle/
```

No separate frontend build step is needed: the frontend lives in `ui-reader/` and is embedded directly.

## Architecture

Frontend (`ui-reader/`) is a vanilla HTML/CSS/JS single page. The Rust backend (`src-tauri/`) exposes a small, capability-gated IPC surface:

| Command | Description |
|---|---|
| `desktop_info` | App version / platform info |
| `pick_folder` | Pick a library root folder |
| `list_md_tree` | List `.md` files under a root (name + mtime sorted) |
| `read_text` | Read a file (path-cage enforced) |
| `load_roots` / `save_roots` | Persist registered library roots (`roots.json` in app config dir) |
| `open_in_terminal` | Spawn an external terminal at a registered root |

### Security model

- **Path cage**: every file read canonicalizes the root and rejects paths that escape it
- **Least privilege**: IPC permissions are declared in `src-tauri/capabilities/default.json`; the WebView has no network access permissions
- **No embedded shell**: terminals open as separate OS processes; the WebView never holds a PTY

## Third-party notices

See [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md) for bundled frontend libraries.

## License

[MIT](LICENSE) © 2026 nakamotosai

## Status & Progress (progress tracker · 2026-08-08 added)

> Tracked via `skill://sai-long-project` + `skill://sai-closeout`. Full session log in `progress.md`; portfolio card in vps `~/project-portfolio/projects.md`.

- **Status**: 暂时完结 (paused, 2026-08-06 08-06 暂收口)
- **Latest**: v1.2.1 published (drag/perf fixed, GH Release shipped, homepage open-source listed, reviewer 8/8)
- **Open residuals**: ① first-screen black/编辑遮挡 ② version number ③ transparency ④ background color `go` ⑤ toolbar two modes ⑥ overlay + version auto-publish
- **Next**: review residuals after user feedback
- **Mirrors**: GitHub (SSOT) + Gitea (self-hosted), dual-repo same HEAD (verify with `git ls-remote`)

---

Mirrors: [GitHub](https://github.com/nakamotosai/sai-md-reader) · Gitea (self-hosted)
